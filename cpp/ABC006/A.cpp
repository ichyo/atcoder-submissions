#include <bits/stdc++.h>
#define REP(i,n) for(int i=0; i<(int)(n); ++i)

using namespace std;

int main(){
    int a, b;
    //cin >> a >> b;
    string s;
    cin >> s;
    cout << s << endl;
    cout << (a % 3 == 0 ? "YES" : "NO") << endl;
    return 0;
}


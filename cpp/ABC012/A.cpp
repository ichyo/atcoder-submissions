#include <bits/stdc++.h>
#define REP(i,n) for(int i=0; i<(int)(n); ++i)

using namespace std;

int main(){
    int a, b, c;
    cin >> a >> b >> c;
    string s;
    cin >> s;

    int ans;

    cout << b << " " << a << endl;
    return 0;
}

